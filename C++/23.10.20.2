#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main()
{
	int a[3];

	int sum = 0;
	float avg = 0;

	int i;
	for (i = 0; i < n; i++)
	{
		printf("score", (i + 1));
		scanf("%d", &a[i]);
	}

	for (i = 0; i < n; i++)
	{
		sum += a[i];
	}

	avg = (float)sum / n;
	printf("avg=%.2f", avg);
	return 0;
}
